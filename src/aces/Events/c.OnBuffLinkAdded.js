export const config = {
  listName: "On buff link added",
  displayText: "On buff link added",
  description: "Fires when a new buff link is registered.",
  isTrigger: true,
  params: [],
};

export const expose = false;

export default function () {
  return true;
}
